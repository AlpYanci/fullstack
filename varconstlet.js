/*const name = 'alp';// var name = 'alp';
// name='zeypi';
alert(name);

//dokumantasyon
/*
    var ile tanımlanaan bir değiştkeni daha sonra ezebiliriz.
    name='zeypi'; daha önce var ile tanımlaaan var name='alp'; içindeki alp ' i ezdi
    var ekrana alert olarak zeypi yazdırdır.
    const ile tanımlanana alp(var yerine) ise sabit bir değer. name='zeypi' ile ezilmedi.

*/
/* let ile ilgili çalışma aşağıda. */
/* const ve var ile tanımladğımız objeler html içinde window.name örneğinde olduğu gibi
window un bir propu dur. fakat let tanımlarsak büüyk projelerde çakışma olmaz. */
if (true) {
    let name='alp';

    alert(name);
    
}